/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The console and labs are fully client-interactive; the landing/docs pages
  // are static. Nothing exotic is needed for Vercel — Next.js is auto-detected.
};

export default nextConfig;
